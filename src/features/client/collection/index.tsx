import { Wrapper } from "@src/common/styled";
import { collectionRoute } from "./route";

const CollectionRoutes = () => {
  return <Wrapper>{collectionRoute()}</Wrapper>;
};

export default CollectionRoutes;
